query RecList($LevelIndex: Int!) {
  getReplaysByLevelIndex(LevelIndex: $LevelIndex) {
    ReplayIndex
    UploadedBy
    LevelIndex
    ReplayTime
    Uploaded
    RecFileName
    UUID
    DrivenBy
    TAS
    Bug
    Nitro
    Finished
  }
}
