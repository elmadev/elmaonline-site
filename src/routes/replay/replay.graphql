query replayQuery($UUID: String!) {
  getReplayByUuid(UUID: $UUID) {
    DrivenBy
    LevelIndex
    RecFileName
    UUID
    ReplayTime
    Uploaded
    Comment
    TAS
    Unlisted
    Finished
    UploadedByData {
      Kuski
    }
    DrivenByData {
      Kuski
    }
  }
}
