query replayQuery($UUID: String!) {
  getReplayByUuid(UUID: $UUID) {
    DrivenBy
    LevelIndex
    RecFileName
    UUID
    ReplayTime
    Uploaded
    Comment
    UploadedByData {
      Kuski
    }
    DrivenByData {
      Kuski
    }
  }
}
